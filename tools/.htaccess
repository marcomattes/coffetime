# Diese Dateien gehören nicht ins Web. Sicherheitsnetz, falls das
# Dokumentenverzeichnis versehentlich auf den Projektstamm zeigt.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
